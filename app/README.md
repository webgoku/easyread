# app/ — Prototipo funzionante

> TODO: scrivere setup e uso quando l'app è pronta.

Struttura:

- `streamlit_app.py` — UI principale
- `orchestrator.py` — coordina gli agenti
- `llm_client.py` — client LLM astratto (OpenRouter / Ollama)
- `models.py` — modelli dati condivisi (Pydantic)
- `agents_impl/` — implementazione Python degli agenti
- `samples/` — documenti AdE di esempio (fittizi)
- `.env.example` — configurazione LLM
- `requirements.txt` — dipendenze Python
